<div class="row">
  <div class="col-sm-12 col-md-12 col-lg-3">
    <p>Listas</p>
    <ul class="list-group">
      <%= render "shared/lists" %>
    </ul>
  </div>
  <div class="col-sm-12 col-md-12 col-lg-9">
    <p>Don't forget</p>

    <%= @list.name %>
    <table class="table">
      <tr>
        <th>Description</th>
        <th>Status</th>
      </tr>
    <% @list.tasks.each do |task| %>
      <tr>
        <td><%= task.description %></td>
        <td><%= task.status %></td>
      <tr>
    <% end %>
    </table>
    <% if current_user == @list.user %>
    <%= current_user.inspect %>
    <\ br>
    <%= @list.user.inspect %>
      <%= link_to 'Edit', edit_list_path(@list), class: "btn btn-primary" %>
    <% else %>
      <%= link_to 'UnFollow', follow_path(user_id: current_user.id, list_id: @list.id), method: :delete, class: 'unfollow-link' %>
    <% end %>
  </div>
</div>
